# 国内镜像：Electron 与 electron-builder 二进制资源下载
# 安装 Electron 时（@electron/get）会读取 electron_mirror
electron_mirror=https://npmmirror.com/mirrors/electron/
# electron-builder 会读取 NPM_CONFIG_ELECTRON_BUILDER_BINARIES_MIRROR
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
